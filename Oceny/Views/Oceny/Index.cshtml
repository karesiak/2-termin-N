@model Dictionary<string, List<Oceny.Models.Ocena>>

<h2>Lista ocen</h2>
<a asp-controller="Oceny" asp-action="AddOcena" class="btn btn-primary">Dodaj nową ocenę</a>


@foreach (var grupa in Model)
{
    <h3>@grupa.Key</h3>
    <table class="table">
        <thead>
            <tr>
                <th>Nr albumu</th>
                <th>Ocena</th>
                <th>Data wpisania</th>
            </tr>
        </thead>
        <tbody>
            @foreach (var item in grupa.Value)
            {
                <tr class="@(item.WartoscOceny == 2 ? "text-danger" : item.WartoscOceny == 5 ? "text-success" : "text-dark")">
                    <td>@item.NrAlbumu</td>
                    <td>@item.WartoscOceny</td>
                    <td>@item.DataWpisania.</td>
                </tr>
            }
        </tbody>
    </table>
}
